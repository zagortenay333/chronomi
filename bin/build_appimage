#!/bin/bash
set -u

cd "${BASH_SOURCE%/*}"

mkdir -p AppDir/usr/bin

cp -r ../data AppDir
cp AppRun AppDir
cp ../kronomi AppDir/usr/bin
cp ../data/kronomi.png AppDir
cp ../data/kronomi.desktop AppDir

./linuxdeploy-x86_64.AppImage --appdir AppDir --plugin gtk --output appimage

wait

rm -rf AppDir
